'use strict';

/**
 * back-ground-screen service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::back-ground-screen.back-ground-screen');
